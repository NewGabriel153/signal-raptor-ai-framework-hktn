# Root .env — consumed by docker-compose.yml via variable interpolation.
# Copy to .env and fill in your API keys.

# ── LLM Provider Keys (forwarded into api & worker containers) ──
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=